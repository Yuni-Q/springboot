spring:
  datasource:
    url: jdbc:mysql://database-1.cbopabdh50kn.ap-northeast-2.rds.amazonaws.com/chocopie?characterEncoding=UTF-8&serverTimezone=UTC&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
    username: admin
    password: chocopie
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: true
    generate-ddl: true
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true